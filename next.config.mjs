/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com',"cdn.pixabay.com",'images.unsplash.com'],

      },
  
};



export default nextConfig;
